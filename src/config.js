/**
 * Configuración central de la aplicación.
 * API_URL apunta al backend simulado (JSON Server).
 * Si JSON Server se mueve de puerto, solo hay que cambiarlo aquí.
 */
export const API_URL = "http://localhost:3001";